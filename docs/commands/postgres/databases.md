---
layout: default
title: postgres databases
description: List databases in a project PostgreSQL instance.
---

# `omurga postgres databases`

```bash
sudo omurga postgres databases ./demo
sudo omurga postgres databases ./demo --instance primary
```

The command runs a read-only database listing through the project container.
