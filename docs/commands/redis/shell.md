---
layout: default
title: redis shell
description: Open an interactive Redis CLI.
---

# `omurga redis shell`

```bash
sudo omurga redis shell ./demo
sudo omurga --host production redis shell ./demo --instance cache
```

Exit with `QUIT` or `Ctrl-D`.
