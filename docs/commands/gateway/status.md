---
layout: default
title: gateway status
description: Check whether the Caddy service is active.
---

# `omurga gateway status`

```bash
sudo omurga gateway status
sudo omurga gateway status --json
```

Use [gateway validate]({{ '/commands/gateway/validate/' | relative_url }}) when the service is active but a route still
does not work.
