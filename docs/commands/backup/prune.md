---
layout: default
title: backup prune
description: Apply backup retention and remove expired snapshots.
---

# `omurga backup prune`

```bash
sudo omurga --dry-run backup prune ./demo
sudo omurga backup prune ./demo
```

Prune only after confirming that the repository and retention policy are
correct.
