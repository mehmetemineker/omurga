---
layout: default
title: backup list
description: List Restic snapshots in a project repository.
---

# `omurga backup list`

```bash
sudo omurga backup list ./demo \
  --password-file /etc/omurga/backup/demo.password
```

Use this to choose a snapshot before running `backup restore`.
