---
layout: default
title: registry add
description: Add or update a Docker registry profile.
---

# `omurga registry add`

```bash
omurga registry add ghcr ghcr.io --username my-user
omurga registry add private registry.example.com --username deploy
```

This stores registry metadata, not the registry password.
